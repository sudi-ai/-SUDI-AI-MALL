# 苏迪 AI 商城 — 增量二开架构基线

开发分支：`sudi-ai-dev`
底座：CRMEB v6.0.0 完整源码

## 不可破坏边界

CRMEB 继续唯一负责商品确定价格、库存扣减、订单状态、支付、退款、拼团、砍价、秒杀、优惠券、会员、分销、客服与其他既有交易能力。

苏迪 AI 层只做智能入口、理解、搜索、推荐、辅助决策和运营分析，不直接修改价格、库存、支付或退款等确定性交易状态。

## 第一阶段能力状态

已开放：

- AI 商品搜索
- AI 购物代理
- AI 尺码助手
- AI 搭配购买
- AI 客服分流

已完成基础服务、暂不对商城前台开放：

- AI 店长诊断（待接入 adminapi 并沿用管理员权限体系）

规划中：

- 图搜商品
- AI 首页统一入口

## 代码隔离原则

新增后端能力优先放入 `crmeb/app/services/sudi/`，通过独立服务接入现有 API/后台，不侵入 CRMEB 核心交易 Service。

`SudiAiGuardService` 采用白名单策略：未知动作默认拒绝。未来任何可能进入执行层的 AI 动作都必须先经过 Guard；价格、库存、订单、支付、退款等交易动作不允许由 AI 层直接执行。

## 接口与权限原则

- 公共商品搜索、购物推荐和搭配接口遵循 CRMEB 的跨域与商城启停中间件。
- 尺码助手和 AI 客服必须经过用户 Token 认证。
- AI 店长不得放在商城前台 API 中；正式开放时必须放入 `adminapi` 并使用 CRMEB 管理员认证/权限中间件。
- 用户身体数据和客服输入当前仅用于本次请求计算，不在 SUDI AI 服务层新增持久化存储。

## 兼容性

CRMEB 当前 `composer.json` 要求 PHP >= 7.1，因此苏迪 AI 新增代码必须保持 PHP 7.1 语法兼容，不使用 PHP 7.4 typed properties、arrow functions 等高版本语法。

## 发布原则

所有开发先进入 `sudi-ai-dev`；完成阶段功能后进行至少 5 轮检查、修复和复审，稳定后再通过 PR 合入 `master`。
